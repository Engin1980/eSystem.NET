using System;
using System.Collections.Generic;
using System.Linq;
using System.Windows.Forms;

namespace Demo
{
  static class Program
  {
    /// <summary>
    /// The main entry point for the application.
    /// </summary>
    [STAThread]
    static void Main()
    {      

      //Person p = new Person();
      //p.Jmeno = "Hubba";
      //p.Prijmeni = "Bubba";

      //System.IO.FileStream fs = new System.IO.FileStream(@"C:\cuc.xml", System.IO.FileMode.Create);

      //ESystem.XML.Serialization.SimpleXMLSerializer ser = 
      //  new ESystem.XML.Serialization.SimpleXMLSerializer();

      //ser.Serialize(fs, p);

      //fs.Close();

      //return;

      //Person p = null;

      //System.IO.FileStream fs = new System.IO.FileStream(@"C:\cuc.xml", System.IO.FileMode.Open);

      //ESystem.XML.Serialization.SimpleXMLSerializer ser =
      //  new ESystem.XML.Serialization.SimpleXMLSerializer();

      //p = ser.Deserialize<Person>(fs);

      //fs.Close();

      //return;

      //Application.EnableVisualStyles();
      //Application.SetCompatibleTextRenderingDefault(false);
      //Application.Run(new Form1());
    }
  }
}
